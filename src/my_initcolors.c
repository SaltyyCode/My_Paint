#include "../include/paint.h"

void init_colors(color_s *colors)
{
    init_red(colors);
    init_pink(colors);
    init_yellow(colors);
    init_green(colors);
    init_blue(colors);
    init_white(colors);
    init_black(colors);
}